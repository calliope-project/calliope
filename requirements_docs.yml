name: calliope-docs

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python=3.11
  - sphinx~=5.3
  - ruamel.yaml
  - nbconvert # To generate HTML files from example notebooks
  - pip
  - pip:
      - readthedocs-sphinx-search
