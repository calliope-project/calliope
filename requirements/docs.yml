name: calliope-docs

channels:
    - conda-forge
    - anaconda

dependencies:
    - python=3.5  # As of June 2017, readthedocs does not support Python 3.6
    - sphinx>=1.5
    - numpydoc
    - ruamel.yaml<=0.15
    - nbconvert  # To generate HTML files from example notebooks
