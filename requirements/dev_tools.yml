name: calliope

channels:
    - conda-forge

dependencies:
    - flake8
    - line_profiler
    - memory_profiler
    - pylint
    - snakeviz
    - twine
    - pip:
        - cufflinks>=0.14  # Needed to build the tutorial notebooks
        - pyprof2calltree
        - gprof2dot
