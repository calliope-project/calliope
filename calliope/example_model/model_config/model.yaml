##
# IMPORT OTHER FILES
##

# Can either be a relative path, absolute path, or one that starts with {{ module }} to access the module directory

import:
    - 'techs.yaml'
    - 'locations.yaml'

##
# OBJECTIVE FUNCTION
##

# If this is not defined, 'constraints.objective.objective_cost_minimization' is used by default
# objective:

##
# ADDITIONAL CONSTRAINTS
##

# List of additional constraints to load
constraints:
    - constraints.ramping.ramping_rate

# Other model-wide settings
system_margin:
    power: 0
    heat: 0