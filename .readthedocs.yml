version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    create_environment:
    - asdf plugin add pixi
    - asdf install pixi latest
    - asdf global pixi latest
    install:
    - pixi install -e docs --frozen
    build:
      html:
      - pixi run docs-build $READTHEDOCS_OUTPUT
mkdocs:
  configuration: mkdocs.yml
