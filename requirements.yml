name: calliope

channels:
    - conda-forge

dependencies:
    - bottleneck
    - coverage >= 4.4
    - codecov
    - glpk = 5.0
    - hdf5
    - libnetcdf
    - pre-commit
    - pytest-cov
    - pytest-xdist  # pytest distributed testing plugin
    - pytest

