name: calliope

channels:
    - conda-forge
    - anaconda

dependencies:
    - python=3.6
    - bottleneck
    - click
    - glpk=4.61
    - hdf5
    - libnetcdf
    - netcdf4
    - numexpr
    - numpy=1.14.1
    - pandas=0.22
    - plotly
    # temporarily installing pyomo via pip
    # - pyomo=5.2
    - pytest
    - pytest-cov
    - ruamel.yaml<=0.15
    - scikit-learn
    - scipy
    - xarray=0.10.1
    - pip:
        - pyomo==5.4.3
